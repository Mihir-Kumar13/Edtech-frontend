apiVersion: v1
kind: Service
metadata:
  name: edtech-frontend-service
spec:
  type: LoadBalancer
  selector:
    app: edtech-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
